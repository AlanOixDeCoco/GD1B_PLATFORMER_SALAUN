export default class SaveManager {
    constructor(){
        
    }

    SaveGame(){
        // Create the new save file
        // Save it to localStorage on top of last save
        // if connected save it to the cloud
    }

    OnGameover(){
        // Remove the save
        // if connected update the player account stats
    }
}